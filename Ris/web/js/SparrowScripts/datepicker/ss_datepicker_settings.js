<!--
	// Interface Related

	var ssdp_type=0;					// 0 - iFrame mode; 1 - popup window mode; 2 - static mode
	var ssdp_fixedX=-1;					// -1 : relative x to activator; 0 : absolute x location
	var ssdp_fixedY=-1;					// -1 : relative y to activator; 0 : absolute y location
	var ssdp_language='en';				// language of datepicker
	var ssdp_scriptdir='/RIS/js/SparrowScripts/datepicker/';			// folder where the scripts are stored
	var ssdp_popupWidth=250;			// width of the datepicker
	var ssdp_popupHeight=210;			// height of the datepicker
	var ssdp_showToday=true;			// show "Today is ..." at the bottom of the datepicker?
	var ssdp_showWeekNumber=false;		// show week number beside every week
	var ssdp_startAt=1;					// 0 - sunday ; 1 - monday
	var ssdp_scrollRate=80;				// rate of scrolling
	var ssdp_useGraphics=true;			// use graphics?

	// Data Related

	var ssdp_minYear=1900;				// minimum year to select
	var ssdp_maxYear=2050;				// maximum year to select
	var ssdp_enablePast=-1;				// enable user to select dates before today?
	var ssdp_enableWeekend=true;		// enable user to select weekends?
	var ssdp_enableHoliday=true;		// allow holidays to be selected?

	var ssdp_dateFormat="dd-mmm-yyyy";		// date format of the datepicker
	var ssdp_todayFormat="dd-mmm-yyyy";		// date format of the today's date

	var ss_yearSplit = 40;	// interprete from this value to 99 as 19xx to 1999 instead of 20xx to 2099.

//-->
